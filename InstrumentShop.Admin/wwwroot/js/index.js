document.getElementById('logout-button').addEventListener('click', function () {
    // Xử lý logout (ví dụ: xóa session, token)
    window.location.href = '/Home/Login'; // Chuyển hướng đến trang Login
});